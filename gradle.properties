# Ativa o AndroidX (obrigatório para bibliotecas modernas)
android.useAndroidX=true
# Converte automaticamente libs antigas para AndroidX
android.enableJetifier=true
# Otimiza a compilação (opcional)
org.gradle.daemon=true
org.gradle.parallel=true